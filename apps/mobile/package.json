{
  "name": "@lingualink/mobile",
  "version": "0.1.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "build": "tsc",
    "eas-build-post-install": "cd ../.. && pnpm --filter @lingualink/types build && pnpm --filter @lingualink/config build && pnpm --filter @lingualink/realtime-events build"
  },
  "dependencies": {
    "@lingualink/config": "workspace:*",
    "@lingualink/realtime-events": "workspace:*",
    "@lingualink/types": "workspace:*",
    "expo": "^54.0.0",
    "livekit-client": "^2.0.0",
    "react": "19.1.0",
    "react-native": "0.81.5"
  },
  "devDependencies": {
    "@babel/core": "^7.20.0",
    "@types/react": "~19.1.10",
    "@types/react-native": "^0.73.0",
    "babel-preset-expo": "~54.0.10",
    "typescript": "^5.3.3"
  }
}
